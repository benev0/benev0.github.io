name = "blog"
version = "1.0.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
# description = ""
# licences = ["Apache-2.0"]
# repository = { type = "github", user = "", repo = "" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
lustre = ">= 5.0.2 and < 6.0.0"
simplifile = ">= 2.2.1 and < 3.0.0"
gleam_regexp = ">= 1.1.1 and < 2.0.0"   # kill if not used
tom = ">= 1.1.1 and < 2.0.0"
filespy = ">= 0.6.0 and < 1.0.0"
gleam_erlang = ">= 0.34.0 and < 1.0.0"
gleam_otp = ">= 0.16.1 and < 1.0.0"
shellout = ">= 1.7.0 and < 2.0.0"       # used to execute commands (keep for colored output)
mist = ">= 4.0.7 and < 5.0.0"
wisp = ">= 1.6.0 and < 2.0.0"
gleam_http = ">= 4.0.0 and < 5.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
lustre_ssg = { git = "https://github.com/lustre-labs/ssg.git", ref = "v0.11.0" }
